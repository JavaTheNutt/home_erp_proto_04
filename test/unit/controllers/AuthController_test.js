const chai = require('chai');
const expect = chai.expect;
const sinon = require('sinon');
chai.use(require('sinon-chai'));

describe ('auth controller -- unit', function () {
  const req = {
    headers: []
  }
})
